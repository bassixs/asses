# Статус проекта и дорожная карта

Единый «блокнот» по состоянию проекта: что сделано, что живёт на сервере, что предстоит.
Обновлено: 2026-07-17.

---

## 1. Короткая картина

Проект — обработка упражнений ассессмент-центра: аудио → расшифровка → роли → оценка
поведенческих индикаторов → заполнение блокнота наблюдателя → отчёт участника + ИПР.

**Сейчас идёт пивот: Telegram-бот → веб-сайт.** Причина — сервер (Москва) не может достучаться
до серверов Telegram (провайдер режет доступ к Telegram; общий интернет работает). Из-за этого у
бота ломались все операции с файлами. Сайт убирает Telegram полностью: файлы грузятся в браузере.

- **Бот** собран, работает как продукт, **заморожен** (тег `bot-v1.0`) — можно вернуть в любой момент.
- **Сайт** — бэкенд и фронтенд готовы, **развёрнут и живой** на сервере (пока временно).

---

## 2. Почему пивот (корень проблемы)

Диагностировано по логам контейнера `telegram-bot-api`:
- `getUpdates` (сообщения) кое-как работает через одно долгоживущее соединение;
- `getFile`/скачивание файлов — виснет: соединения к дата-центрам Telegram (в т.ч. файловому DC)
  либо не устанавливаются, либо крайне медленно;
- прямая проверка: **все IP Telegram недоступны** с сервера (таймаут 45с), а обычный интернет
  (8.8.8.8, github, AI Tunnel) — мгновенно.

Вывод: **провайдер блокирует/душит Telegram** (типично для РФ, усилилось в начале июля). В коде это
не чинится. Отключение IPv6 и рестарты не помогли (заблокирован и IPv4). Правильные пути: прокси/VPN
к Telegram, перенос сервера — **или уход от Telegram = сайт** (выбрано).

---

## 3. Что заморожено — бот `bot-v1.0`

Полнофункциональный бот (ветка `bot-legacy`, тег `bot-v1.0`). Умеет:
- гайд-флоу кнопками: центр → участник (приватный код) → упражнение → инструкции → аудио/блокнот;
- STT через AI Tunnel Whisper (+ чанкование по размеру и длительности);
- разметка ролей (ведущий/участник) с учётом инструкций упражнения;
- анализ блокнота, статусы +/−/НЗ, уровни по формуле ТЗ, таймкоды цитат, проверка цитат;
- **4 формата упражнений** проверены (Беседа, Планирование, Саморазвитие, Аналитический кейс);
- **групповое упражнение** + загрузка уже заполненного блокнота;
- отчёт DOCX/PPTX + ИПР, библиотека компетенций/упражнений, сводный отчёт по нескольким упражнениям;
- устойчивость к подвисаниям (ретраи скачивания/отправки, мягкие уведомления).

Восстановить бота: `git checkout bot-v1.0` → развернуть как раньше (нужен рабочий канал к Telegram).

---

## 4. Сайт — что готово

Живёт в `main`, код — в `web/` (бэкенд `web/backend`, фронтенд `web/frontend`). Переиспользует
всё ИИ-ядро бота без изменений: `bot/services/*`, `bot/models/*`, `bot/resources/*`. Заменён
только слой доставки (Telegram → REST + загрузка файлов).

### Бэкенд (FastAPI) — `web/backend/`
- `app.py` — приложение, HTTP Basic-авторизация, раздача собранного SPA.
- `deps.py` — сессия БД, sentinel-владелец для веб-строк.
- `schemas.py` — pydantic-схемы.
- `processing.py` — конвейер (расшифровка→роли→анализ→блокнот, чтение заполненного, сборка
  отчёта+LLM-персонализация, рендер DOCX/PPTX/ИПР).
- `jobs.py` — фоновая обработка аудио + статус.
- `routers/` — `assessments` (CRUD), `files` (загрузки), `reports` (статус, отчёт, ИПР, скачивания).
- **17+ эндпоинтов**, документация — `/docs`.

### Фронтенд (React + Vite + TypeScript) — `web/frontend/`
- SPA: центр → участник → упражнение; страница результата; скачивания.
- Шаг упражнения повторяет гайд-флоу (инструкции, выбор способа, загрузки, опрос статуса).
- **Редизайн (2026-07-13):** тёмная тема с лаймовым акцентом и свечением, «стеклянные» карточки,
  шрифт Manrope, hero-блок, hover-анимации — современный вид в стиле референса заказчика.
- **Переработка UX (2026-07-17):** навигация по разделам — **Обзор** (hero + «Как это работает» из
  4 шагов + карточки разделов + живые мини-метрики), **Центры** (рабочее пространство),
  **Аналитика**, **FAQ** (аккордеон: формула уровней, статусы +/−/НЗ, роли, приватность, форматы,
  групповые упражнения, проверка цитат). Цель — HR понимает продукт с первого экрана.
- **Аналитика** — эндпоинт `GET /api/overview` (счётчики + средний уровень + распределение по
  уровням ТЗ + средние по компетенциям). Графики нарисованы чистым SVG/CSS (gauge, горизонтальные
  бары, сегментная полоса распределения) — без внешних библиотек, с ховер-подсказками.
- **Каталог упражнений (2026-07-17):** упражнения создаются заранее в разделе «Упражнения», а на
  этапе оценки только выбираются из готовых (вручную названия больше не вводятся).
- **Демо-режим** (`demo.ts`): мок-API для витрины без бэкенда (GitHub Pages / `?demo=1`),
  обогащён под аналитику и каталог (три состояния упражнения).

### Каталог упражнений и гейт понимания

Модели `ExerciseTemplate` + `ExerciseTemplateMaterial`, `exercises.template_id`
(миграция `20260717_0013`). Жизненный цикл карточки упражнения:

1. **Черновик** — HR создаёт упражнение, прикладывает материалы (PDF/DOCX/TXT/MD, текст
   извлекается и копится в `instructions_text`) и **пустой блокнот наблюдателя** (.xlsx).
2. **Проверка ИИ** (`POST /exercise-templates/{id}/check`) — сервис `exercise_understanding`
   отдаёт модели материалы вместе с компетенциями/индикаторами блокнота и получает карточку
   понимания: суть, формат, роли ведущего и участника, ожидаемые ситуации, замеряемые
   компетенции, **что замерить нельзя**, правила «НЗ», список пробелов и вердикт `understood`.
   Жёсткие гарды: без материалов или без блокнота `understood` всегда `false`.
3. **Активация** — только HR и только при `understood=true` (иначе 400). Проверено:
   гейт держится на сервере, а не только кнопкой в UI.
4. **Инвалидация** — любая догрузка материала или замена блокнота сбрасывает вердикт,
   снимает с публикации и требует повторной проверки.

Выбирается в оценке только при `is_usable` = активировано **и** есть блокнот.
При создании упражнения участнику имя и инструкции **снапшотятся** (прошлые результаты не
меняются задним числом), а на блокнот каталога заводится строка `ObserverNotebook` — поэтому
конвейер обработки (аудио → роли → анализ → отчёт) не менялся вообще.

Встроенная библиотека (4 упражнения) импортируется в каталог при старте, идемпотентно,
как **готовая** (`understood=true`, активирована), но с `is_usable=false` — у них нет файла
блокнота, его прикладывает HR. Сидинг обёрнут в try/except: его падение не мешает сайту стартовать.

### Проверено на сервере (смоук-тест, реальные данные)
- CRUD, загрузка **заполненного блокнота** (уровни читаются из правильных ячеек — подтверждено),
  агрегация, **вызовы LLM к AI Tunnel**, генерация **DOCX-отчёта (48 КБ)** и **ИПР (41 КБ)** — всё 200.
- Фронтенд собирается чисто (`tsc && vite build`), развёрнут, отвечает снаружи.

---

## 5. Где это живёт (инфраструктура)

- **Сервер:** `85.239.35.73` (Ubuntu 24.04, root), Москва. Нода `kvmnvm-930`.
- **Python-окружение:** `/opt/asses/.venv` (общее с ботом; доустановлены fastapi/uvicorn/multipart/httpx).
- **Node:** v20 (LTS).
- **Бот:** `/opt/asses`, systemd-сервис `asses-bot` (сейчас бесполезен из-за блокировки Telegram).
- **Сайт:** `/opt/asses-web` (git-worktree, detached HEAD на `origin/main`), собранный фронт в `web/frontend/dist`.
- **Запуск сайта (сейчас):** uvicorn `0.0.0.0:8000` через `nohup`, лог `/var/log/asses-web.log`.
- **БД:** `/opt/asses/data/app.db` (общая), миграции применены. Загрузки — `/opt/asses/data/uploads`,
  результаты — `/opt/asses/data/reports`.
- **Запуск сайта:** systemd-сервис **`asses-web`** (active + enabled, `Restart=always`,
  обёртка `/opt/run-asses-web.sh`) — переживает падения и ребут.
- **Доступ к сайту:** **https://hr40.ru** (nginx-реверс-прокси → 127.0.0.1:8000) — Basic-auth, логин любой,
  пароль = `ADMIN_BOT_PASSWORD` (`1172`). Прямой `http://85.239.35.73:8000` тоже жив.
- **Домен/HTTPS:** `hr40.ru` (reg.ru), A-запись → `85.239.35.73`. Сертификат **Let's Encrypt** на
  `hr40.ru`+`www.hr40.ru`, http→https редирект, автопродление таймером certbot. nginx-конфиг
  `/etc/nginx/sites-available/hr40` (`client_max_body_size 300M`, `proxy_read/send_timeout 1800s`).
- **Firewall:** ufw активен, порты `80,443,8000/tcp` открыты.
- **⚠️ Хостинг флапает:** провайдер интермиттентно режет ВХОДЯЩИЕ соединения (и веб :8000, и SSH :22
  отвечают ~6 из 10 раз). Тот же корень, что и блокировка Telegram. Для боевого сайта нужен переезд
  на нормальный хостинг.
- **Прочее:** IPv6 отключён (`/etc/sysctl.d/99-disable-ipv6.conf`); cron рестарта `telegram-bot-api`
  каждые 6ч (наследие бота, для сайта не нужен).

### Демо на GitHub Pages (визуальная витрина)
- **URL: https://bassixs.github.io/asses/** — всегда доступен (CDN GitHub), в отличие от сервера.
- Это фронтенд в **демо-режиме** (`src/demo.ts`): мок-данные в памяти, «обработка» имитируется,
  скачивание отключено, сверху плашка. Включается на `*.github.io` или через `?demo=1`.
- Автодеплой: workflow `.github/workflows/pages.yml` — собирает и публикует при каждом пуше
  в `main`, если менялся `web/frontend/**`. Pages настроен на build_type=workflow, ветка `main`
  разрешена в environment `github-pages`.

### Обновить сайт на сервере
```bash
cd /opt/asses-web && git fetch -q origin main && git reset --hard origin/main
# если менялся фронтенд: cd web/frontend && npm install && npm run build
systemctl restart asses-web
```

⚠️ **Если в обновлении есть миграция** — применить ДО рестарта, обязательно с абсолютным
путём к БД. В `/opt/asses/.env` путь относительный (`./data/app.db`), и из воркбокса
`/opt/asses-web` alembic не найдёт базу («unable to open database file»), миграция молча не
применится, а сервис потом упадёт на старте:
```bash
cd /opt/asses-web
set -a; . /opt/asses/.env; set +a
export DATABASE_URL="sqlite+aiosqlite:////opt/asses/data/app.db"
export DOWNLOAD_DIR="/opt/asses/data/uploads"
cp /opt/asses/data/app.db /opt/asses/data/app.db.bak   # бэкап перед миграцией
/opt/asses/.venv/bin/alembic upgrade head
```

---

## 6. Что предстоит

### Продакшн-обвязка
- [x] **systemd-сервис** для сайта (`asses-web`, автозапуск + рестарт) — сделано 2026-07-13.
- [x] **Демо-витрина на GitHub Pages** с автодеплоем — сделано 2026-07-13.
- [ ] **Переезд на нормальный хостинг** — текущий провайдер флапает по входящим (см. раздел 5).
- [x] **Домен + nginx + HTTPS** (Let's Encrypt) — сделано 2026-07-16: `https://hr40.ru`, редирект, автопродление.
- [ ] Выделенная БД/каталоги для сайта (или осознанно оставить общие с ботом).

### Функционал / доводка
- [x] **Живой тест аудио-пути на сайте** — E2E 2026-07-14: 59 МБ аудио → 5 минут до готового
      блокнота (инструкции, шаблон, расшифровка, роли, анализ, скачивание) — всё через веб-API.
- [x] **Тест сводного отчёта** по 2 упражнениям — работает; найден и исправлен дефект: компетенции
      с суффиксом «(дополнительный замер)» дублировались в сводке → нормализация имён при агрегации.
- [x] **Групповой HR-блокнот** («Татьяна открытие», 77 индикаторов) прочитан через веб с ручными
      уровнями — путь «заполненный блокнот» подтверждён на реальном файле.
- [ ] Авторизация: сейчас HTTP Basic (один пароль). При желании — страница логина/пользователи/роли.
- [x] **Понятность для HR** — сделано 2026-07-17: разделы (Обзор/Центры/Аналитика/FAQ),
      «Как это работает», FAQ, дашборд с графиками.
- [x] **Каталог упражнений + гейт понимания ИИ** — сделано 2026-07-17: упражнения создаются
      заранее с материалами и блокнотом, ИИ подтверждает понимание, HR активирует; на оценке —
      выбор из готовых. E2E проверено на сервере (реальный блокнот 89 индикаторов).
- [ ] UI-полировка: индикаторы загрузки, обработка ошибок, список/удаление, множественные инструкции,
      выбор формата отчёта на странице, мобильная вёрстка.
- [ ] Прогресс обработки аудио сейчас в памяти (один процесс) — при нескольких воркерах/рестарте
      теряется; при масштабировании вынести в БД/очередь.

### Решения
- [ ] Судьба бота: оставить замороженным / удалить сервис / вернуть, если появится канал к Telegram.
- [x] **Влить `web` в `main`** — сделано 2026-07-17: `main` = основной продукт (сайт), `web` удалена.

---

## 7. Git — структура

- **`main`** — единственная активная ветка и **основной продукт (сайт)**. Default на GitHub.
  Вся разработка и деплой идут отсюда.
- `bot-legacy` — ветка-закладка на замороженного бота.
- **`bot-v1.0`** — тег: неизменяемый снимок рабочего бота.

Ветка `web` слита в `main` (fast-forward) и удалена 2026-07-17 — сайт стал основным
продуктом, держать две одинаковые ветки незачем. Историю разработки сайта видно в `main`.

Репозиторий: `github.com/bassixs/asses`.

---

## 8. Ключевые заметки

- Вся ИИ-логика (STT, роли, анализ, отчёты, ИПР, библиотеки) — **общая**, живёт в `bot/services`
  и переиспользуется и ботом, и сайтом. Меняем логику один раз — работает в обоих.
- `bot/config.py`: бот-специфичные креды (`BOT_TOKEN`, Yandex) сделаны необязательными (дефолт `""`),
  чтобы сайт импортировал сервисы без них.
- Расшифровка и LLM идут через **AI Tunnel** — канал к нему с сервера рабочий (в отличие от Telegram).
- Приватность: участник хранится по коду/номеру, без ФИО.
